/******/ (() => { // webpackBootstrap
/******/ 	var __webpack_modules__ = ({

/***/ "./assets/ts/metabox-design/metabox-design.ts":
/*!****************************************************!*\
  !*** ./assets/ts/metabox-design/metabox-design.ts ***!
  \****************************************************/
/***/ (function() {

var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var __spreadArray = (this && this.__spreadArray) || function (to, from, pack) {
    if (pack || arguments.length === 2) for (var i = 0, l = from.length, ar; i < l; i++) {
        if (ar || !(i in from)) {
            if (!ar) ar = Array.prototype.slice.call(from, 0, i);
            ar[i] = from[i];
        }
    }
    return to.concat(ar || Array.prototype.slice.call(from));
};
/** FAB_METABOX_DESIGN */
window.FAB_METABOX_DESIGN = __assign(__assign({}, window.FAB_METABOX_DESIGN), { 
    /** Linked Option (Padding/Margin) */
    linkedOptions: {
        padding: true,
        margin: true
    }, 
    /**
     * Define Default Option
     * */
    defaultOptions: __assign(__assign({}, window.FAB_METABOX_DESIGN.defaultOptions), { select2: { placeholder: '--choose--' } }), 
    /** Init */
    init: function () {
        /** Initiate Select2 */
        jQuery('.fab-container.metabox-design .select2').select2();
        /** Initiate Color Picker */
        jQuery('.fab-container.metabox-design .colorpicker').wpColorPicker();
        /** Initiate Option */
        window.FAB_METABOX_DESIGN.init_option_animation(); // Init option Animation.
        window.FAB_METABOX_DESIGN.init_option_modal_size(); // Init option Modal Size.
        window.FAB_METABOX_DESIGN.init_option_spacing(); // Init option spacing
        window.FAB_METABOX_DESIGN.init_option_layout(); // Init option theme
        window.FAB_METABOX_DESIGN.init_option_theme(); // Init option theme
        window.FAB_METABOX_DESIGN.init_option_template(); // Init option template
        /** Trigger Linked Option */
        jQuery('.fab-linked-option').on('click', window.FAB_METABOX_DESIGN.triggerLinkedOption);
    }, 
    /** Init Animation Option */
    init_option_animation: function () {
        /** Initiate Animation */
        jQuery('.field_option_animation_element').each(function () {
            /** Ignore Animation FAB */
            if (jQuery(this).attr('id') === 'field_option_animation_fab')
                return true;
            /** Set Animation */
            var animationDOM = jQuery(this);
            animationDOM.select2({
                data: window.FAB_PLUGIN.animateCSSAnimation
            });
            var selected = animationDOM.data('selected');
            if (selected) {
                animationDOM.val(selected);
                animationDOM.trigger('change');
            }
        });
        /** Additional Animation FAB */
        var animationDOM = jQuery('#field_option_animation_fab');
        var data = __spreadArray([], window.FAB_PLUGIN.animateCSSAnimation, true);
        data.splice(1, 0, { id: 'ripple', text: 'Ripple' });
        animationDOM.select2({ data: data });
        var selected = animationDOM.data('selected');
        if (selected) {
            animationDOM.val(selected);
            animationDOM.trigger('change');
        }
    }, 
    /** Init Template Option */
    init_option_template: function () {
        var dom = jQuery('#field_option_design_template_shape');
        dom.select2({
            placeholder: '--choose--',
            data: window.FAB_PLUGIN.defaultOptions.template.shape
        });
        dom.val(dom.data('selected'));
        dom.trigger('change');
    }, 
    /** Init Modal Size */
    init_option_modal_size: function () {
        var dom = jQuery('#field_option_design_modal_size');
        dom.select2({
            placeholder: '--choose--',
            data: window.FAB_METABOX_DESIGN.defaultOptions.size.type
        });
        dom.val(dom.data('selected'));
        dom.on('select2:select', window.FAB_METABOX_DESIGN.triggerDesignSize);
        dom.trigger('change');
        window.FAB_METABOX_DESIGN.triggerDesignSize();
    }, 
    /** Init Option Spacing */
    init_option_spacing: function () {
        /** Sizing Value */
        jQuery('.fab_modal_layout_spacing_sizing').each(function () {
            var dom = jQuery(this);
            var sizing = dom.data('selected');
            if (sizing) {
                dom.val(sizing);
                dom.trigger('change');
            }
        });
        /** Set Initial linkedOptions */
        var spacing = { padding: [], margin: [] };
        jQuery('.fab-modal-layout-spacing').each(function () { spacing[jQuery(this).data('layout')].push(jQuery(this).val()); });
        window.FAB_METABOX_DESIGN.linkedOptions.padding = spacing.padding.every(function (val, i, arr) { return val === arr[0]; });
        window.FAB_METABOX_DESIGN.linkedOptions.margin = spacing.margin.every(function (val, i, arr) { return val === arr[0]; });
        jQuery('.fab-linked-option').each(function () {
            window.FAB_METABOX_DESIGN.set_linked_spacing_button(this);
        });
        /** Trigger Change */
        jQuery('.fab-modal-layout-spacing').on('keyup', function () {
            var layout = jQuery(this).data('layout');
            if (window.FAB_METABOX_DESIGN.linkedOptions[layout]) {
                jQuery(".fab-modal-layout-".concat(layout)).val(jQuery(this).val());
            }
        });
    }, 
    /** Init Option Layout */
    init_option_layout: function () {
        var dom = jQuery('#field_option_design_modal_layout_id');
        dom.select2({
            placeholder: '--choose--',
            data: window.FAB_METABOX_DESIGN.defaultOptions.layout
        });
        dom.val(dom.data('selected'));
        dom.trigger('change');
    }, 
    /** Init Option Theme */
    init_option_theme: function () {
        var dom = jQuery('#field_option_design_modal_theme');
        dom.select2({
            placeholder: '--choose--',
            data: window.FAB_METABOX_DESIGN.defaultOptions.theme
        });
        dom.val(dom.data('selected'));
        dom.trigger('change');
    }, 
    /** Trigger Design Size */
    triggerDesignSize: function () {
        var dom_custom_size = jQuery('#setting_design_custom_size');
        var size = jQuery('#field_option_design_modal_size').val();
        if (size === 'custom') {
            dom_custom_size.show();
        }
        else {
            dom_custom_size.hide();
        }
    }, 
    /** Trigger Linked Option */
    triggerLinkedOption: function () {
        var layout = jQuery(this).data('layout');
        window.FAB_METABOX_DESIGN.linkedOptions[layout] = !window.FAB_METABOX_DESIGN.linkedOptions[layout];
        window.FAB_METABOX_DESIGN.set_linked_spacing_button(this);
    }, 
    /** Set Button Icon */
    set_linked_spacing_button: function (dom) {
        var layout = jQuery(dom).data('layout');
        var icon = window.FAB_METABOX_DESIGN.linkedOptions[layout] ? 'fa-link' : 'fa-unlink';
        jQuery('em', dom).removeClass('fa-link fa-unlink');
        jQuery('em', dom).addClass(icon);
    } });


/***/ })

/******/ 	});
/************************************************************************/
/******/ 	
/******/ 	// startup
/******/ 	// Load entry module and return exports
/******/ 	// This entry module is referenced by other modules so it can't be inlined
/******/ 	var __webpack_exports__ = {};
/******/ 	__webpack_modules__["./assets/ts/metabox-design/metabox-design.ts"]();
/******/ 	
/******/ })()
;
//# sourceMappingURL=metabox-design.min.js.map